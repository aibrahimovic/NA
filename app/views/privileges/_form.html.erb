<span id="notice"><%= notice %></span>

<h4>Nova privilegija</h4>
<br>

<br><br>
<%= form_for(privilege) do |f| %>
  <% if privilege.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(privilege.errors.count, "error") %> prohibited this privilege from being saved:</h2>

      <ul>
      <% privilege.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="row">
    <div class="col-md-6">

      <div class="form-group row">
     
        <%= f.label :name, 'Naziv', :class => "col-xs-3 col-form-label"%>
        <div class="col-xs-9">
          <%= f.text_field :name, :class => "form-control"%>
        </div>
      </div>

      <div class="actions row">
        <div class="col-xs-3 col-form-label"></div>
        <div class="col-xs-9">
          <%= f.submit 'Kreiraj privilegiju', :class => 'btn btn-primary' %> 
        </div>
      </div>
      </div>

  <div class="col-md-6">
  </div>

<% end %>
