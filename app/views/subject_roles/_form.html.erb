<%= form_for(subject_role) do |f| %>
  <% if subject_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subject_role.errors.count, "error") %> prohibited this subject_role from being saved:</h2>

      <ul>
      <% subject_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
