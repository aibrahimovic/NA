<%= form_for(student_number) do |f| %>
  <% if student_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_number.errors.count, "error") %> prohibited this student_number from being saved:</h2>

      <ul>
      <% student_number.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subject_id %>
    <%= f.number_field :subject_id %>
  </div>

  <div class="field">
    <%= f.label :predicted_student_number %>
    <%= f.number_field :predicted_student_number %>
  </div>

  <div class="field">
    <%= f.label :student_number %>
    <%= f.number_field :student_number %>
  </div>

  <div class="field">
    <%= f.label :academic_year %>
    <%= f.text_field :academic_year %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
