<%= form_for(student_number) do |f| %>
  <% if student_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_number.errors.count, "error") %> prohibited this student_number from being saved:</h2>

      <ul>
      <% student_number.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class = "row">
  <div class="col-md-6">


  <div class="form-group row">
    <%= f.label :broj_studenata, :class => "col-xs-3 col-form-label" %>
    <div class="col-xs-9">
      <%= f.number_field :student_number %>
    </div>
  </div>

  <div class="actions row">
    <div class="col-xs-3 col-form-label"></div>
    <div class="col-xs-9">
      <%= f.submit 'Spasi izmjene', :class => 'btn btn-primary' %> <br>
      <%= link_to 'Nazad', student_numbers_path %>
    </div>
  </div>


<% end %>
