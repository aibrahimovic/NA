<%= form_for(ensemble) do |f| %>
  <% if ensemble.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ensemble.errors.count, "error") %> prohibited this ensemble from being saved:</h2>

      <ul>
      <% ensemble.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subject_id %>
    <%= f.number_field :subject_id %>
  </div>

  <div class="field">
    <%= f.label :teacher_id %>
    <%= f.number_field :teacher_id %>
  </div>

  <div class="field">
    <%= f.label :academic_year %>
    <%= f.text_field :academic_year %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

