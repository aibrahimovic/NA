<%= form_for(academic_year) do |f| %>
  <% if academic_year.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(academic_year.errors.count, "error") %> prohibited this academic_year from being saved:</h2>

      <ul>
      <% academic_year.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class = "row">
  <div class="col-md-6">

  <div class="form-group row">
    <%= f.label :status, :class => "col-xs-3 col-form-label"%>
    <div class="col-xs-9">
      <%= f.number_field :status %>
    </div>
  </div>
    
  <div class="actions row">
    <div class="col-xs-3 col-form-label"></div>
    <div class="col-xs-9">
      <%= f.submit 'Spasi izmjene', :class => 'btn btn-primary' %> 
    </div>
    </div>
  </div>
<% end %>
