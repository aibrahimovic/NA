<span id="notice"><%= notice %></span>

<h4>Uloge u sistemu</h4>
<br>
<%= button_to 'Kreiraj novu ulogu', new_user_path, class: 'btn btn-default', :method => :get %>
<br><br>
<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @roles.each do |role| %>
      <tr>
        <td><%= role.name %></td>
        <td><%= link_to 'Izmijeni', edit_role_path(role) %></td>
        <td><%= link_to 'Obriši', role, method: :delete, data: { confirm: 'Da li ste sigurni?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

